openapi: 3.2.0
info:
  title: Verato Delete Relationship Service API
  version: 2026.1.2
  description: 'Operations tagged DeleteRelationshipService across 2 of this provider''s published API definitions: verato-organization-openapi.yml, verato-person-openapi.yml. Each path carries the servers of the definition it was published in.'
servers:
- url: https://yourveratodomain.com/org-link-ws/svc
  description: Production Organization Link API
- url: https://yourveratodomain.com/link-ws/svc
  description: Production Link API
security:
- basicAuth: []
tags:
- name: DeleteRelationshipService
paths:
  /deleteRelationshipService:
    post:
      summary: Delete a relationship (deleteRelationshipService)
      description: Deletes a previously defined relationship between two identities.
      operationId: deleteRelationship
      requestBody:
        required: true
        description: The delete relationship request.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ServiceRequest_deleteRelationshipService'
      responses:
        '200':
          description: Successful delete relationship operation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServiceResponse_deleteRelationshipService'
      tags:
      - DeleteRelationshipService
    servers:
    - url: https://yourveratodomain.com/org-link-ws/svc
      description: Production Organization Link API
    - url: https://yourveratodomain.com/org-link-ws/svc
      description: Sandbox Organization Link API
components:
  schemas:
    ServiceResponse_deleteRelationshipService:
      type: object
      properties:
        auditId:
          type: string
        success:
          type: boolean
        warnings:
          type: array
          items:
            type: string
        retryableError:
          type: boolean
        message:
          type: string
        content:
          $ref: '#/components/schemas/DeleteRelationshipWsResponse'
        errors:
          type: array
          items:
            type: string
        trackingId:
          type: string
    DeleteRelationshipWsResponse:
      type: object
      properties:
        id:
          type: string
    DeleteRelationshipWsRequest:
      type: object
      properties:
        id:
          type: string
    ServiceRequest_deleteRelationshipService:
      type: object
      properties:
        content:
          $ref: '#/components/schemas/DeleteRelationshipWsRequest'
        trackingId:
          type: string
  securitySchemes:
    basicAuth:
      type: http
      scheme: basic
x-refined-from:
- verato-organization-openapi.yml
- verato-person-openapi.yml
